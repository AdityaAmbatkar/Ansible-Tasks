- name: web deployment
  hosts: all
  become: yes
  tasks: 
    - name: install package
      ansible.builtin.package:
        name:

  tasks:
    - name: copy file from one locatio to another location
      ansible.builtin.copy:
        src: /root/index.html
        dest: /var/www/html
